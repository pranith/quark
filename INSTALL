Please install all prerequisties to build qemu. Then run setup.sh.

On Debian/Ubuntu based linux distributions, please run

```
$ sudo apt-get build-dep qemu
$ ./setup.sh
```

For RISCV/ARM64, please install the corresponding cross compilers

$ sudo apt-get install gcc-aarch64-linux-gnu g++-aarch64-linux-gnu gcc-riscv64-linux-gnu g++-riscv64-linux-gnu
